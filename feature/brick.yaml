name: flood_feature
description: Brick to generate a Flood feature.
repository: https://github.com/JLogical-Apps/flood-brick/tree/master/feature

version: 4.0.0+1

environment:
  mason: ">=0.1.0-dev.52 <0.1.0"

vars:
  feature_name:
    type: string
    description: The name of the feature (Names the ValueObject, Entity, and Repository)
    default: User
    prompt: What is the name of the feature?